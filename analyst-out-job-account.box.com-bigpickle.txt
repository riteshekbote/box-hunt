[0m
> build · big-pickle
[0m
# BOX BUG BOUNTY — Scope Analysis

## Key Findings

### ✅ In-Scope Hosts (8 total)
| Host | Purpose | Phase |
|------|---------|-------|
| `upload.box.com` | File upload pipeline | PLATFORM |
| `notes.services.box.com` | Box Notes backend | PLATFORM |
| `m.box.com` | Mobile web app | WEB |
| `dl.boxcloud.com` | File download CDN | CDN |
| `cloud.app.box.com` | Embedded content views | PLATFORM |
| `app.box.com` | Primary web app | WEB |
| `api.box.com` | Core API | API |
| `account.box.com` | Auth plane | AUTH |

### 🚫 Hard Forbidden
- `signrequest.com` — **HONEYPOT** (visiting bans IP)
- `sr-staging-1.com` — Partner staging, out of scope
- Any `*.box.com` content outside the 8 hosts above

### 📋 Rules Summary
1. **READ-ONLY**: GET/HEAD/OPTIONS only — no POST/PUT/PATCH/DELETE
2. **No account actions**: No creation, uploads, writes, OAuth submission
3. **Politeness**: 1 req/2s, ≤60/cycle, specific User-Agent
4. **Fail-stop**: First 4xx/429/403 → stop probing that host
5. **No redirects**: Never follow to third-party origins
6. **Phased approach**: RECON → SURFACE → HYPOTHESIS → POC

### ⚠️ Important Notes
- Documentation sites (`developer.box.com`, `support.box.com`) are readable for research
- AUTH_HELPED/HUMAN_ONLY items are parked — don't attempt without explicit permission
- All hypotheses must include confidence scores and evidence requirements

---

**What specific analysis or next steps do you need help with?**
