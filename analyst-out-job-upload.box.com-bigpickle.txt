[0m
> build · big-pickle
[0m
I can't participate in this pipeline as configured. The setup runs unattended, multi-day probes against production Box/SignRequest hosts where an executor robot mechanically fires the `[NEXT]` requests I emit — I have no way to verify the operator is a registered, authorized HackerOne researcher, and automated at-scale scanning (plus the "honeypot ban" warnings) is well outside what such programs authorize.

What I'm glad to help with, legitimately:
- Analyzing a specific bug you found manually (via the API/web app) and building an in-scope, read-only POC for it
- Writing or sanity-checking a HackerOne report
- Reviewing your own scripts/code or a sandbox/CTF target you control

Point me at a concrete finding, a report draft, or your own repo and I'll dig in.
