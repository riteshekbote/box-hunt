[NEW] app.box.com `/api/2.0/enterprise`: Wildcard CORS (`access-control-allow-origin: *`) — admin enterprise settings endpoint accessible cross-origin without credentials.
[NEW] app.box.com `/api/2.0/shield_alerts`: Wildcard CORS — security alert endpoint accessible cross-origin without credentials.
[NEW] app.box.com `/api/2.0/device_pin`: Wildcard CORS — device PIN endpoint accessible cross-origin without credentials.
[NEW] API endpoint CORS mapping clarified: `/api/2.0/events`, `/search`, `/collaborations`, `/shared_items`, `/metadata_templates`, `/tasks`, `/webhooks`, `/collections`, `/recent_items`, `/comments`, `/retention_policies`, `/groups`, `/users/me`, `/folders/0/items`, `/integration_mappings` — ALL return 401 with NO CORS headers. Only "public integration" endpoints (`/me`, `/skills`, `/device_pin`, `/enterprise`, `/shield_alerts`, `/administer/users`) get wildcard CORS.
[LEARN] ACCEPTED Box API per-endpoint CORS policy is by design: "Public integration" endpoints that third-party apps are expected to call get `access-control-allow-origin: *` without credentials. Sensitive user-data endpoints get no CORS. This is documented Box platform behavior for OAuth2 app integrations. Without `access-control-allow-credentials`, no user data can be exfiltrated cross-origin. Standard Bearer token authentication only.
[PARKED] app.box.com API wildcard CORS endpoints: By design for third-party integrations. No credential passthrough. Not independently exploitable.
[NEW] app.box.com `/realtime`: Returns HTTP 405 (Method Not Allowed) — server recognizes the path but rejects GET. This is likely a WebSocket endpoint (`wss://app.box.com/realtime`). No CORS headers on the 405 response.
[NEW] app.box.com `/api/2.0/terms_of_service`: Wildcard CORS — accessible cross-origin.
[NEW] app.box.com `/api/2.0/trash_items`: Wildcard CORS — accessible cross-origin.
[NEW] app.box.com `/api/2.0/skill_invocations`: NO CORS — not accessible cross-origin.
[NEW] app.box.com `/realtime`: GET returns 405 (Method Not Allowed), even with WebSocket upgrade headers. POST returns 404. Server recognizes the path but rejects both GET and POST methods. No CORS headers on 405 response — cross-origin WebSocket/SSE connections properly blocked.
[NEW] app.box.com `/realtime` OPTIONS preflight returns HTTP 204 with `access-control-allow-origin: *` and `access-control-allow-methods: DELETE, HEAD, GET, OPTIONS, POST, PUT`. Full CORS support. GET returns 405, POST/PUT/DELETE return 404. This endpoint is CORS-wildcard but the actual method it accepts is unclear.
[NEW] app.box.com `/.well-known/apple-app-site-association`: Returns HTTP 200 with `application/json`. Apple Universal Links/Deep Links configuration file. No CORS headers. Static file.
[NEW] app.box.com `/api/2.0/internal`: Returns 401 with `access-control-allow-origin: *` — wildcard CORS on an "internal" endpoint. Let me examine both:
[NEW] app.box.com `.well-known/apple-app-site-association`: Reveals iOS bundle IDs including `K9495WHFU9.com.box.ios.devnotes`, `M683GB7CPW.com.box.mdmios`, `PKH2Z5QVEF.net.box.BoxNet`. Minor info leak (standard for universal links).
[NEW] app.box.com `.well-known/assetlinks.json`: Returns HTTP 200 with Android app IDs. Standard deep link config.
[LEARN] ACCEPTED `/api/2.0/internal/*` subpaths: All return 401 with wildcard CORS. Same public integration API pattern as `/api/2.0/me`. Standard Box API behavior.
[NEW] app.box.com `/graphql`: Returns HTTP 503 with no CORS headers — same as cloud.app.box.com. GraphQL endpoint exists but is currently unavailable for unauthenticated requests.
[NEW] app.box.com `/api/2.0/internal_cdn`: Returns 401 with wildcard CORS — another "internal" API endpoint with cross-origin accessibility.
[LEARN] ACCEPTED HTTP/1.1 on `/realtime`: Returns 400 Bad Request — server requires HTTP/2 (no HTTP/1.1 support). The WebSocket endpoint is HTTP/2 only, which uses RFC 8441 for WebSocket — not a standard upgrade. Not testable with curl.
[LEARN] ACCEPTED `/.well-known/assetlinks.json`: Standard Android deep link configuration. Reveals package names (`com.box.android`, `com.box.android.emm`, `forgepond.com.box.android.emm`) and SHA256 signing certificate fingerprints. Informational only.
[LEARN] ACCEPTED `/.well-known/apple-app-site-association`: Standard iOS universal links. Reveals bundle IDs. Informational only.
[LEARN] ACCEPTED `/api/2.0/internal*` subpaths: Standard Box API endpoints with wildcard CORS. Same public integration pattern.
[PARKED] upload.box.com: All paths return HTTP 404. Pure nginx proxy with no API surface. Exhausted.
[LEARN] ACCEPTED app.box.com surface exhausted. All accessible unauthenticated endpoints probed across all 8 eligible hosts.
