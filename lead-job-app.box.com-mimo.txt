[NEW] accounts.box.com → accounts.app.box.com → accounts.account.box.com: 3 previously undiscovered subdomains revealed through redirect chains. accounts.account.box.com is a distinct login-origin with its own SPA (Box | Login), cookie domain `.account.box.com`, and CORS origin reflection.
[NEW] accounts.account.box.com OAuth2 SPA shell CORS reflection: Same pattern as confirmed finding on cloud.app.box.com/account.box.com — reflects arbitrary Origin in `access-control-allow-origin`, exposes `WWW-Authenticate,Server-Authorization`. This is the 6th subdomain with identical behavior. No `access-control-allow-credentials`.
[NEW] accounts.account.box.com API per-endpoint CORS policy: `/api/2.0/me` and `/api/2.0/skills` return `access-control-allow-origin: *`, while `/api/2.0/folders/0` and `/api/2.0/users/me` return 401 with NO CORS headers. Confirms Box applies per-endpoint CORS on API — public integration endpoints get wildcard, sensitive data endpoints get none. Same pattern on app.box.com.
[LEARN] ACCEPTED OAuth2 redirect_uri is client-side validated only: On all SPA-catch-all origins (app.box.com, cloud.app.box.com, accounts.account.box.com), the `/api/oauth2/authorize` endpoint returns 200 (SPA shell) for ANY `redirect_uri`, including `https://evil.com/callback`. The SPA validates client_id + redirect_uri client-side. No server-side redirect occurs during the initial authorize request — safe.
[LEARN] ACCEPTED dl.boxcloud.com, upload.box.com, notes.services.box.com: No API surface — all return 404. dl.boxcloud.com is nginx download-proxy. upload.box.com is nginx. notes.services.box.com is Express with CSRF but no CORS.
[LEARN] ACCEPTED app.box.com/api/2.0/folders/0: Returns 401 with NO CORS headers — not all Box API endpoints have wildcard CORS. Per-endpoint policy confirmed.
[PARKED] dl.boxcloud.com: All paths return 404. No CORS-relevant surface discovered. Download proxy only.
[PARKED] m.box.com: 301 → app.box.com. No unique surface.
[PARKED] notes.services.box.com: Express app with CSRF, no CORS on any endpoint. Properly secured.
[PARKED] upload.box.com: nginx, all paths 404. No CORS headers.
[NEXT] Attempt to find `accounts.account.box.com` unique paths that may serve the login SPA (potentially with different Box.config than the enduserapp SPA) — e.g., GET https://accounts.account.box.com/account/recover with Origin: https://evil.com — check if new login-origin SPA has different embedded data than the standard enduserapp SPA.
