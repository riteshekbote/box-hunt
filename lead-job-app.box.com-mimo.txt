[CONFIRMED] app.box.com /api/2.0/me wildcard CORS with Authorization header: Returns `access-control-allow-origin: *` regardless of Authorization header presence. No `access-control-allow-credentials` header. `vary: Origin` present but CORS remains wildcard. Standard OAuth error response (401 invalid_token) — no user data leaked. Confirms previous [PARKED] assessment: this is standard third-party API integration behavior, not a vulnerability.
[NEXT] GET https://app.box.com/api/2.0/folders/0 with headers: `Origin: https://evil.com, Authorization: Bearer test-token` — check if folder listing endpoint has wildcard CORS or different behavior than /me
[NEXT] GET https://dl.boxcloud.com/api/2.0/files/content with headers: `Origin: https://evil.com, Authorization: Bearer test-token` — check if download CDN has different CORS behavior than upload CDN
[LEARN] ACCEPTED CORS reflection confirmed on account.box.com OAuth2 SPA shell: `access-control-allow-origin: evil.com` + `access-control-expose-headers: WWW-Authenticate,Server-Authorization` + cookies on `.box.com` domain. The `Server-Authorization` exposed header is custom and potentially interesting.
[NEXT] GET https://account.box.com/api/oauth2/authorize?response_type=code with headers: `Origin: https://evil.com` — capture full response body and check for `Server-Authorization` header value
[NEXT] GET https://account.box.com/api/2.0/users/me with headers: `Origin: https://evil.com` — check if account.box.com has API endpoints with different CORS behavior than the SPA shell
[NEW] account.box.com/api/oauth2/token: Returns 400 with `access-control-allow-origin: *` — wildcard CORS on token endpoint, standard OAuth2 behavior.
[NEW] app.box.com/app-api/enduserapp/current-user: Returns HTTP 200 with `x-enduserapp-currentversion: 23.692.0+hotfix.1` — version information leak. Sets cookies on `.app.box.com` domain.
[NEW] **Admin endpoints have wildcard CORS!** Both `/api/2.0/administer/users` and `/api/2.0/enterprise/audit_logs` return `access-control-allow-origin: *` on 401. This is unusual for admin-only endpoints.
[NEW] **account.box.com/login** also reflects Origin in CORS! `access-control-allow-origin: https://evil.com` + `access-control-expose-headers: WWW-Authenticate,Server-Authorization`. The SPA shell catch-all reflects Origin on ANY path, not just OAuth2 authorize.
[NEW] **e3.boxcdn.net** returns 403 with S3 XML error — reveals it's an S3 bucket in `us-east-1`. The bucket exists but doesn't allow listing.
[LEARN] ACCEPTED Admin/enterprise API endpoints on app.box.com have `access-control-allow-origin: *` with full method set (DELETE, HEAD, GET, OPTIONS, POST, PUT) and Authorization header. This is by design for third-party API integrations — the Box API uses Bearer tokens and allows cross-origin requests from any origin. Without `access-control-allow-credentials`, cookies are not sent cross-origin, so this is not exploitable for cookie theft. However, any Bearer token obtained through other means (token theft, XSS, social engineering) could be used cross-origin to access admin data.
[LEARN] ACCEPTED account.box.com/login and app.box.com/login reflect Origin in CORS — this is the SPA shell catch-all behavior, not specific to the OAuth2 authorize endpoint. Any path that serves the SPA shell reflects the Origin header.
[LEARN] ACCEPTED e3.boxcdn.net is an S3 bucket in us-east-1, properly secured (403 on all paths). No data leakage.
[LEARN] ACCEPTED x-enduserapp-currentversion header leaks app version (23.692.0+hotfix.1) on app.box.com and cloud.app.box.com — not cross-origin readable (no CORS headers on the response).
[LEARN] ACCEPTED XSS attack surface properly mitigated — server JSON-encodes all parameter values before embedding in `<script>` tag. The `\"` escaping prevents JavaScript injection.
[NEXT] Probing for any remaining novel attack surfaces — check for WebSocket hijacking, SSRF, or other injection types on the 8 eligible hosts
[NEW] `/api/2.0/skills` has wildcard CORS — same as `/api/2.0/me`. This is another Box AI endpoint with cross-origin accessibility.
[NEW] notes.services.box.com Socket.IO returns HTTP 200 with content (117 bytes) but NO CORS headers. Confirmed: cross-origin JS cannot read the response.
[LEARN] ACCEPTED all findings. The OAuth2 SPA shell CORS reflection is the primary confirmed finding. The admin endpoint CORS policy is by design for the Box API platform. No new high-confidence exploitable issues found this cycle.
